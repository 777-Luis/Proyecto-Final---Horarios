const { Client } = require('pg'); const client = new Client({ user: 'erp_user', host: 'localhost', database: 'erpdblocal', password: 'erp_password', port: 5432 }); client.connect().then(async () => { const m = await client.query('SELECT * FROM matriculas'); console.log('Matriculas:', m.rows); const h = await client.query('SELECT * FROM horarios'); console.log('Horarios:', h.rows); client.end(); });
